---
type: review-index
updated: 2026-08-10
---

# Review Index

One line per pass — detail lives in each pass's review/resolution/findings files; lessons and
numbers in [rationale.md](rationale.md). The old essay-style rows are in this file's git
history. Findings = new 🔴/🟠/🟡/⚪. Dormant targets live under `archive/`.

## Targets at a glance

| Target | State |
|---|---|
| 044-045 observability | **Archived 2026-08-10.** **CLOSED 2026-08-10 by owner sign-off** (`a4eb7e5`) — six passes, five fix rounds, **0 🔴 and 0 🟠 open at close**; the v5 round was judged patch-grade (three documents, one panel caption, one doc comment, one test), so no delta discovery and **no certification pass ran** — recorded per README note ²: a lower-tier loop may close on sign-off without a fresh full-manifest pass, which means no commit of this feature has been blind-searched since the v1 discovery at `5cac465`. Open and accepted: **D46** — SLO 1 still counts `/health` and `/metrics`, so the availability panel cannot read below ~94.5% (owner-parked, needs .NET 9's `IHttpMetricsTagsFeature.MetricsDisabled`; the panel and `slos.md` now say so in operator-readable text) — plus 🟡/⚪ backlog rows D111, D116, D117 and D124. **Two manifest lenses never ran on this target — `db-parity` and `frontend-ux`, owed and not waived**; judged low value here (no entity, query or migration in the bolt; no frontend file). Re-arms only on a new serious finding in these files. <br><br> **v6 verification 2026-08-10** (`a4eb7e5`, tip `6cae573`) — **approve-with-followups. 4 of 4 fixes verified, 0 declined, 0 reopened**, both mutation predictions matched. D121 proven twice: deleting the SLO 4 guard from both copies reddens the new test, and so does the round's own A/B — a deleted dashboard panel that a duplicated doc copy tries to cover, which the fixer's *first* version let pass. D110/D122/D123 are prose with no build check, so each claim was read against the code it describes: every `Skipped(...)` site (5 benign + the orphaned one), the `LogError` marker and level, the shipped SLO 4 matchers, the per-attempt counter, and all three dilution figures recomputed from `scrape_interval: 15s`, `HEALTHCHECK --interval=30s` and `~500 req/day` — with the premise confirmed at `ObservabilityExtensions.cs:98`, where the ASP.NET metrics instrumentation carries **no filter**, so `/health` and `/metrics` really are counted. **1 new finding, ⚪, and it is the residual the fixer disclosed**: the pinned selector list is hand-maintained (D124) — the four pinned selectors are every hand-named one in either file today, but a fifth ships unpinned. Recorded against the fixer's stated reason: a rule keyed on **literal `=` matchers on the numerator side** would cover the class without touching SLO 1 or the error-rate panel (both use `!~`/`=~`), so the blocker was the walker's brace-vs-division ordering, not the two panels. **Nothing re-arms the loop.** 57 deferrals stand, none closes; D117 is half-closed (the unresolvable `Tracked as D46` citation is gone, both unpinned halves stand). **Method deviation recorded: 0 agents** — this session runs under a standing no-subagent instruction, so the runbook's anchored judgment-item agent and lens-owned fix-diff questions were done by the main agent reading HEAD. 282 scoped tests green. **The loop is quiet: no blocker, no serious finding, nothing owed but the owner's word on closing.** <br><br> **v5 fix round resolved 2026-08-10** (`a4eb7e5`) — **4 of 4 fixed, 0 open**, active 68 min, blocked 0 min, no owner gate and no approach-check needed (nothing trigger-list-shaped). The three v5 minors were fixed rather than backlogged because the owner asked the loop to end naturally, and **D110 was cleared in the same round by owner request** — the operator panel and `slos.md` now say ~8,640 self-monitoring requests a day and a ~94.5% floor, each figure naming its source, after three passes of flagging it. **The micro-review broke my own fix and the A/B is the evidence:** my first guard pin counted each selector twice *across* the two files, so duplicating the doc's numerator while deleting the dashboard panel's query left it **green** — measured — and **red** against the same mutation once counting went per source (`a4eb7e5`). One micro-review claim refuted by probe rather than accepted: the count assertion is not empty-vacuous, because the counters are seeded from the selector list, so empty collectors redden (measured). Also swept beyond the finding: the operator log table had **no row at all** for the Error-level `sameday.awb.orphaned` marker while calling that case healthy. Final scoped run 1097 green; branch pushed and the four gates re-run at `a4eb7e5`. **Re-review owed** — nothing here is `verified` until a pass that did not fix it says so. · [resolution](archive/044-045-observability/resolution-v5.md) <br><br> **Owner call 2026-08-07: the v4 fix round is PATCH-GRADE** — no delta discovery. Loop goes quiet after one hand-fix round over the three v5 minors (D121–D123) plus **D110**, the operator-panel figures flagged three passes running; the owner asked the loop to "end naturally" once those are cleared, and closure is still recorded only on their explicit word. <br><br> **v5 verification 2026-08-07** (`52a0cb9`, tip `d37f867`) — **approve-with-followups. 4 of 4 fixes verified, 0 declined, 0 reopened**, and **7 of 7 mutation predictions matched**. The measurement this pass existed for: v4 predicted *and* measured 0-red for "pass `ct` to `GetAsync` instead of the linked deadline token" — the gap that became D104 — and that same mutation now reddens exactly one test, taking **32 s** to do it, which is the 30 s backstop measured. D103's fix verified as a **class rule**, not an instance check: deleting SLO 3's guards reddens it, and so does collapsing its two-term numerator to one matcher, so the test cannot pass by finding nothing. **D113 is verified by CI only, the only place it can be** — `secret-scan` on the `pull_request` event was red at `f0aadd7` and every earlier PR run, green from `a9c9478` (the first commit carrying `.gitleaksignore`) onward; both fingerprints checked byte-for-byte against the commits they name. Durability limit recorded: commit-pinned fingerprints die silently on any history rewrite of this branch. **3 new findings, all 🟡, all fix-caused by D105, all one shape** — the round added a mechanism and left one of its records describing the world before it: the `or vector(0)` guards it added to the SLO 4/5 numerators are unpinned because the new rule skips single-term sides (**measured green on deletion**; disclosed by the fixer, still two new unpinned sites — D121); the acceptance criterion still says SLO 4 excludes only `skipped` and gives `retry_later`'s reason for it (D122); and the outcome union's doc comment still calls the cancelled-order case a plain skip — the one case that must now set `Orphaned: true` (D123). **Nothing re-arms the loop on severity.** Two lens claims dropped rather than filed (the `rate()` inconsistency is D111, already open; and the container test does prove construction with the deadline defaulted), and one lens's line references were unreliable, so every kept claim was re-read at HEAD. Deferral sweep agent-free and mechanical — 57 stand, none closes; **D110's wrong dilution figures are untouched for the third consecutive pass**. 3 anchored lenses / ~175k tok. Auditor clean <br><br> **v4 fix round resolved 2026-08-06** (`52a0cb9`) — **4 of 4 fixed, 0 open. All four CI gates green on both events** (`ci` + `secret-scan`, push + pull_request at `40f3b54`) — the first all-green PR this branch has had, and the proof for F4 that no local run could give. Active 88 min, blocked 21 min across **two** owner gates. **Both micro-reviews found defects in the fixer's own work, and the second broke a fix outright.** F4's rename could not possibly have worked: gitleaks on `pull_request` scans the PR's **commit range**, and `git merge-base --is-ancestor` shows all three commits carrying the token are branch-only — so renaming at HEAD cannot clear a scan that reads history, and the commit message's "low entropy" reason was wrong too (the new value's entropy is *higher*). Re-gated; owner chose a `.gitleaksignore` of the two fingerprints (`243625c`). F3 likewise shipped twice more: it had carried over only *half* of SLO 3's precedent (the exclusion, not the `or vector(0)` numerator guard, so a total AWB failure would read "No Data" instead of a red 0%), and then two verified residuals — `retry_later` counted **per attempt**, so an order succeeding on try 3 scored 1/3 against the very retry loop the 2% budget protects; and `skipped` overloaded across six call sites, one of which returns it right after an `Error`-level "orphaned billable label" log, hiding ops' worst AWB outcome. Both fixed: `retry_later` out of the denominator, and a new `orphaned` result value (cardinality 5→6) implemented as a flag on `Skipped` so the dispatcher's switch and six `BeOfType` assertions keep working. **The first micro-review had already found my F1 guard test could pass while checking nothing** — it now requires a guard per added term on *both* sides of the division and refuses to pass with zero terms examined. Every fix revert-proven; **one red proof was invalid and is recorded as such** (a `$"` in a perl pattern interpolated as a special variable, so the mutation silently never applied — caught because 0 red on a must-pin fix is itself the tell). Final scoped run 1096 green. **And CI then caught a flake in this round's own new test** — the wall-clock deadline proof measured 4s948ms against a 3s bar on `ubuntu-latest`, going red on the push event while passing on the pull_request event at the same SHA; the micro-review had asked exactly this question and the fixer judged the risk small. Third attempt (`52a0cb9`) gives the test its own 30 s backstop and a 15 s bar, so the two paths are 15 s apart instead of 12 s. **Not fixed, recorded:** SLO 5's `failed`/`rejected` likely carry the same per-attempt bias; the Sameday client's 10 s timeout is shorter than its own 21 s retry ladder (filed to [inbox](inbox.md)). **Re-review owed.** · [resolution](archive/044-045-observability/resolution-v4.md) <br><br> **v4 verification 2026-08-06** (`dc203c7`) — **approve-with-followups. 11 of 11 fixes verified, 0 declined, 0 reopened — and the blocker is genuinely gone**: `ci` is green on `ubuntu-latest` at the branch tip after six consecutive red runs, the only platform that could prove D74. Every behaviour and test fix revert-proven (13 mutations, 16 of 17 predictions matched; **the one miss is recorded** — `BindingAddress.Parse` defaults a named pipe to port **80**, not 0, so `IsSocketOrPipe` is load-bearing and both I and the tests lens had it backwards). **The loop re-arms on fix-caused 🟠, and the shape is one thing: mechanisms that are the whole point of a fix, with nothing stopping their removal.** The `or vector(0)` guard the round's own micro-review added — after catching that the new SLO 3 query would read "No Data" while healthy — deletes with **1133 green** (D103). So do both invariants the new Google deadline discriminator rests on, and breaking the `HttpBackstop > RequestDeadline` one **restores D75 invisibly** (D104). So does the owner-decided "widen the alarm, not the re-enqueue" boundary (D108). **A second CI gate was found red, one workflow over from the one review-v3 watched:** `secret-scan` has failed on **every pull-request run of this branch** — nine runs, back past the v2 review commit — because gitleaks flags a fabricated test token `.gitleaks.toml` does not allowlist (D113). Also: SLO 4 and SLO 5 still carry the denominator defect D80 fixed while the rewritten status block says there are "two caveats that matter" (D105); the dilution figures now stamped on the operator's panel are wrong — 5,760/day is `/metrics` alone and the real floor is ~94.5%, not ~99.7% (D110). **3 backlog rows close that were already fixed when recorded** — D97, D100 and D89; `summary-v3` asked the owner to spend attention on D97 while it was closed. 18 new findings (0🔴/4🟠/10🟡/4⚪), 11 fix-caused. 3 anchored lenses / ~490k tok, neither with LSP available. **Loop re-armed → fix round.** <br><br> **v3 fix round resolved 2026-08-06** (`dc203c7`) — **11 of 11 fixed, 0 open. CI is green again**: the 🔴 D74 fix is confirmed on `ubuntu-latest` after six consecutive red runs, which is the only platform that could prove it. **All four approach-checks returned "needs revision" and three prevented a new defect** — most sharply on the blocker, where the draft would have turned today's correct abort for a socket-only host into a silent boot. The second micro-review then caught a defect in the fixer's own work: the new SLO 3 numerator `sum(A) + sum(B)` is *empty* when no duplicate has ever been recorded, so the panel would have read "No Data" in exactly the healthy case (`or vector(0)` on both terms). Every fix revert-proven; final scoped run 1133 green. Active 46 min, blocked 4 min on one batched gate of four owner decisions (all taken as recommended). **Not fixed, recorded:** SLO 4 and SLO 5 carry the same denominator defect F7 fixed — each needs the same definitional call the owner made for SLO 3; AWB recovery is still impossible, only visible; Sentry has no real-hub precedence test. **Re-review owed.** · [resolution](archive/044-045-observability/resolution-v3.md) <br><br> **v3 verification 2026-08-05** (`7e28317`) — **request-changes. 9 of 10 fixes verified, 0 reopened, but CI is RED on this branch and has been since the fix round** (green at `8daa977`, failing all 6 runs since `e791c40`): the F6 guard's own test only passes on Windows, because `BindingAddress.Parse` treats a unix-socket address differently off-Windows, which also defeats the guard's only-listener rule on the deploy platform (🔴 D74). **D45 declined** — a fix whose regression test fails in CI is not verified. Also measured: F4's timeout carve-out is dead code and its test passes on a shape .NET never produces, so a Google outage racing a client abort now returns 200 and reaches neither SLO 1 nor Sentry (D75); the nested-`MetricCapture` throw added by the last round's micro-review has no test (D78); the unmapped-500 log level is still unpinned (D76). 29 new findings (1🔴/10🟠/14🟡/4⚪), 19 fix-caused. **Loop re-armed → fix round.** <br><br> **v2 fix round resolved 2026-08-05** (`7e28317`) — **10 of the 11 🟠 fixed; D46 PARKED by the owner** (deferred, not fixed): the approved fix needs .NET 9, and the two remaining routes each change what SLO 1 measures, so **availability still cannot read below ~99.7%** and the latency/RPS panels stay diluted until it is drained. **Re-review owed.** Details: active 71 min, blocked 5 min on one batched owner gate. **F7/D46 is the open one and needs a second owner decision**: the option approved at the gate (exclude `/metrics` and `/health` at the instrumentation) does not exist on .NET 8 — `IHttpMetricsTagsFeature.MetricsDisabled` is .NET 9 and OTel 1.11's metrics overload takes no options — confirmed by compiling the attempt. The 3 adversarial approach-checks earned their cost: one **refuted** the F6 design outright (a throw from `ApplicationStarted` is swallowed by the host, so the guard would have logged and shrugged) and one caught that the planned F2 test **could not have reddened** because the test file built its own copy of the sampler. Both fix-diff micro-reviews found real defects in the fixer's own work (**15**, all repaired) — including the fixer corrupting `slos.md` (deleting SLO 3's action-on-breach heading) and the F5 fix **re-introducing the very class it was closing** (promising label-value coverage that skips framework instruments). A class-sweep miss the fixer got wrong went to the inbox: the same `== Paid` defect lives in the AWB retry sweep. Renderer bug fixed en route — `endIdx` indexed the wrong array, so it crashed on every round after the first. 1120 scoped tests green · [resolution](archive/044-045-observability/resolution-v2.md) <br><br> **v2 verification 2026-08-05** (`e965c99`) — **22 of 23 fixes verified, 0 reopened**; every one revert-and-rerun proven across 26 mutations, failure sets predicted before each run. **D17 declined**, not verified: its fix is partial (the concurrent double-click leg has no conditional write, no guard, no test) — the resolution asked the re-review to decide and this is the decision. The three per-cluster questions then produced **34 new findings (0🔴/11🟠/14🟡/9⚪)**, **21 of them caused by the v1 fixes** — headline: a redelivered success webhook for an order legitimately past `Paid` now pages ops and burns SLO 3 (D40); a client abort mid Google sign-in becomes a Sentry issue (D43); and four places where a test or a doc asserts a guarantee nothing enforces (D44/D48/D49/D54 — deleting the breadcrumb hook or the `LogError` leaves the suite green, both measured). One 🟠 is a **v1 miss, not a regression**: `ParentBasedSampler` already let an inbound `traceparent` decide sampling at `5cac465` (D41). Loop re-armed → fix round. 6 anchored lenses / ~772k tok. Auditor clean |
| review system (meta) | **v1 system review 2026-07-29** — 14 findings stand (0🔴/5🟠/5🟡/4⚪), 2 refuted by an independent defense pass; headline: the undeviated certification path has never completed, evidence branches never pushed, stop rule certified twice while its gating experiment (seeded run 2) never ran. Recommendations 1–7 await owner |
| 015 sameday shipping | **Archived 2026-08-10.** **CLOSED 2026-07-29 by owner sign-off** (`5734021`) — no post-fix blinded pass ran; recorded per README note ². Path: v5 certified `5fc330b` → 41-fix round → v6 verification (37 verified, **4 reopened**: D27/D39/D71/D79 were correct but *nothing could prove them* — deleting each left both suites green) → v6 test-only fix round closing all 4 plus the D68/D78 gaps, every one revert-proven. v6 also **reproduced** D65's DI cycle: flipping `Sameday:Enabled=true` pre-fix would have thrown on the first courier call. Backend 921 / frontend 460 green (suite 30 s → 4 s once `FakeTimeProvider` stopped sleeping for real). **The pre-enable checklist is the gate, not this closure** — D45 vendor idempotency unconfirmed, D23 Postgres DDL never executed, D81 service ids still `7`, D90–D96 backlog. Dormant behind two false flags |
| 043 cloud storage | **Archived 2026-08-10.** **Certified 2026-07-22** (no serious defect survives; D83–D90 → backlog) — loop complete |
| 042 thumbnail cache | **CLOSED 2026-08-11 by retroactive owner sign-off (quiet since v9, never certified).** Archived. Loop quiet after v9; certification never ran (pre-dates the risk tiers) |
| 035 payment idempotency | **CLOSED 2026-08-11 by retroactive owner sign-off (loop complete at v10, never certified).** Archived. Fix loop complete at v10; never certified (pre-dates the protocol) |

## Passes

| Date | Target | Pass | Verdict | New H/M/L/C | Outcome | Files |
|---|---|---|---|---|---|---|
| 2026-08-10 | 044 | v6 verification (anchored, main-agent only) | approve-with-followups | 0/0/0/1 | **4 of 4 verified, 0 declined, 0 reopened; 2 of 2 predictions matched.** M1 — the SLO 4 `or vector(0)` deleted from both copies — is the mutation **review-v5 measured green**, and it now reddens exactly one test with 281 others green. M2 re-runs the fix round's own A/B independently: delete the dashboard's AWB panel, duplicate the doc's query, and the per-source rule reddens naming `awb_creation_total{result="ok"} in the dashboard` — the case the fixer's first version passed. The three prose fixes carry no build check by design, so each was read against code: `Skipped`'s five benign sites (`AwbCreator.cs:91/95/97/123/264`) plus the orphaned one at `:272`, the `LogError` marker at `:269`, the shipped matchers `result!="skipped",result!="retry_later"`, the per-attempt counter at `:61`, and D110's arithmetic recomputed from `DEPLOYMENT.md:1049` (15 s scrape → 5,760), `Dockerfile:43` (30 s health → 2,880) and `DEPLOYMENT.md:951` (~500 customer req/day) → 8,640 ÷ 9,140 = 94.5%. Premise checked too: `ObservabilityExtensions.cs:98` adds ASP.NET metrics with **no filter**, so both self-monitoring paths are counted. **1 new ⚪ (D124)** — the pinned list is hand-maintained; the four pinned selectors are every hand-named literal-value selector in either file today, but a fifth ships unpinned, and the fixer's stated blocker (a class rule would red SLO 1 and the error-rate panel) does **not** hold for a literal-`=`-matcher rule — those two use `!~`/`=~`. Deliberately still ⚪: writing it needs brace-aware division splitting the two existing walkers do in the opposite order. Vacuity probe not re-run — M2 proves the same mechanism from the other side, recorded. 57 deferrals stand, none closes. **0 agents / ~0 tok** (standing no-subagent instruction; deviation recorded in the review) | [review] · [findings] · [ledger](archive/044-045-observability/ledger.md) · [summary] |
| 2026-08-07 | 044 | v5 verification (anchored, 3 cluster lenses) | approve-with-followups | 0/0/3/0 | **4 of 4 verified, 0 declined, 0 reopened; 7 of 7 predictions matched.** 5 revert-and-rerun proofs + 2 claim probes. **The one v4 could not get:** unwiring the deadline from `GetAsync` — 0-red in v4 across 1133 — now reddens exactly one test **at 32 s**, the 30 s backstop measured against a 15 s bar. D103 verified as a class rule: guards deleted → red, and the two-term numerator collapsed to one `=~` matcher → red on its `addedTermsSeen >= 4` floor, so it cannot pass by checking nothing. D105's production half reddens on dropping `Orphaned: true`, and all six `Skipped` sites were read — five genuinely benign, only the billable-label one flagged. **D113 verified by CI only:** PR-event `secret-scan` red at `f0aadd7` and all earlier PR runs, green from `a9c9478` (first commit with `.gitleaksignore`); all four gates now green on both events. Fingerprints verified byte-for-byte; **durability caveat recorded** — a history rewrite invalidates commit-pinned fingerprints silently. **Measured, not argued:** the guards the same round added to the SLO 4/5 numerators delete green, because the new rule skips single-term sides — the fixer disclosed the hole, and it still leaves two new sites unpinned (D121). Two record-drift findings alongside it: the acceptance criterion still authorises only the `skipped` exclusion while attaching `retry_later`'s reason to it (D122), and the outcome union's doc comment names the now-orphaned cancelled-order case as the benign example (D123). Also measured and *not* filed: negative-matcher label **values** are outside the build-check net (green on a value typo) while their **names** are inside it (red on a name typo) — both exactly as documented. 57 deferrals stand, none closes, swept mechanically from the diff with no agent. 3 agents / ~175k tok | [review] · [findings] · [ledger](archive/044-045-observability/ledger.md) · [summary] |
| 2026-08-06 | 044 | v4 verification (anchored, 3 cluster lenses) | approve-with-followups | 0/4/10/4 | **11 of 11 verified, 0 declined, 0 reopened. review-v3's blocker is gone — `ci` green on `ubuntu-latest` at the branch tip after 6 red runs.** 13 revert-and-rerun proofs plus 3 claim-probes plus 1 positive DI probe; **16 of 17 predictions matched and the miss is recorded**: I predicted deleting `IsSocketOrPipe` would leave the suite green because `Parse` supposedly ports socket/pipe addresses at 0 — it defaults a **named pipe to port 80**, so the helper is load-bearing and the pipe theory case reddens. The tests lens filed the same wrong conclusion as a finding; the measurement refutes both of us. **Measured, not argued — the round's own repair is unpinned:** deleting both `or vector(0)` guards leaves **1133 green**, so the "No Data while healthy" defect the second micro-review caught mid-round can return silently (D103). Same for the two invariants the new Google deadline rests on — passing `ct` instead of the linked token leaves 1133 green (the tests pass via the 15 s backstop, run 38 s vs 20 s), and putting the registered timeout back to 5 s also leaves 1133 green, which **restores D75** because the timers then race (D104). Same for the owner's explicit "widen the alarm query only" boundary (D108). **Second CI gate red, unnoticed through two passes and a fix round:** `secret-scan` fails on all 9 pull-request runs of this branch (passes on every push, which scans only new commits) over a fabricated test token at `SentryDataScrubbersTests.cs:16` that `.gitleaks.toml` does not allowlist (D113). SLO 4/5 still carry D80's denominator defect while the new status block claims "two caveats that matter" (D105); the panel's dilution arithmetic is wrong by ~5 points of floor (D110). **D97, D100 and D89 close — all three were recorded `backlog` while already fixed**, and `summary-v3` sent the owner to look at D97 after it was closed. DI-resolution risk settled green by a throwaway probe, then deleted. 3 agents / ~490k tok; neither lens had `LSP` | [review] · [findings] · [ledger](archive/044-045-observability/ledger.md) · [summary] |
| 2026-08-05 | 044 | v3 verification (anchored, 3 cluster lenses + 1 deferral checker) | request-changes | 1/10/14/4 | **9 verified, 1 declined, 0 reopened — and one 🔴 the fix round shipped: CI has been red on this branch since it.** `ScrapeListenerCheckTests.An_address_with_no_port_is_not_counted_as_a_listener` passes on Windows and fails on `ubuntu-latest`, because `BindingAddress.Parse` strips one character less off-Windows (`GetUnixPipePath` decrements the prefix length), so a unix-socket address returns `Port=0` instead of throwing. CI green at `8daa977`, red on all 6 runs since `e791c40`; the round's own records say `1120/0`, true only on Windows. Same root cause defeats the guard's "scrape port is the only listener" rule on the deploy platform (D74). **D45 declined**, not verified — a fix whose regression test fails in CI is not proven. 11 revert-and-rerun proofs, every predicted failure set matched exactly, zero collateral. Three claim-probes each found a gap: the nested-`MetricCapture` throw the last round's micro-review added has **no test** (deleting it leaves 738 green, D78); the **unmapped**-500 log level is unpinned, i.e. D49 one branch over on the path handling most 500s (D76); F9's breadcrumb test stays green with every breadcrumb dropped (D85). **Measured, not argued:** F4's `GetBaseException() is not TimeoutException` carve-out is **dead code** — a real `HttpClient` probe on net8.0 shows the chain is `TCE → TimeoutException → TCE`, so the base is never a `TimeoutException` and the shipped filter is identical to the naive one it replaced; its regression test passes only on a shape .NET never produces, and a Google outage racing a client abort now returns **200** and reaches neither SLO 1 nor Sentry (D75). The disclosed F2 residual is real: re-wrapping the production call site restores D41 with **1120 green** (D79). Two lens claims corrected on measurement (one downgraded, one upgraded from inference to fact). D57 closed as a side effect of the D44 fix; 23 deferrals re-affirmed incl. the parked D46 — whose file now points a reader the *wrong* way, offering SLO 1 as SLO 3's cross-check while SLO 1 is the diluted one (D81). 4 agents / ~559k tok | [review] · [findings] · [ledger](archive/044-045-observability/ledger.md) · [summary] |
| 2026-08-05 | 044 | v2 verification (anchored, 6 cluster lenses) | approve-with-followups | 0/11/14/9 | **22 verified, 0 reopened, 1 declined.** 26 revert-and-rerun mutations, every predicted failure set matched: deleting `SendDefaultPii=false` reddens 1 and `SentryDataScrubbers.Register` reddens 2 exactly as the fixer reported; the review's own unbounded `user_id` tag reddens through `ContractViolations()`. **D17 declined** — only the commit-ordering leg is proven; the concurrent double-click leg has no guard and no test. All 16 deferrals re-affirmed (D28/D31 narrowed, D30 changed shape — its file is gone, the missing startup log is not). Then the three per-cluster questions: **11 new 🟠**, 8 fix-caused. Measured, not argued: deleting `SetBeforeBreadcrumb` leaves 358 green (D48); reverting `LogError`→`LogWarning` leaves 24 green (D49). D40 is the live one — the new webhook `else` treats `Printing`/`Shipped`/`Delivered` as "customer charged, order not Paid". **Downgraded on re-check:** the tests lens rated `MetricCapture`'s no-op meter filter High and predicted random reds; mechanism confirmed (`FotoMetrics.Meter` is one process-wide static) but 5 consecutive runs of the colliding sets went 133/133 green, so it is 🟠 on the mechanism (D47). **Re-attributed:** D41 pre-dates the fix round — a v1 miss, not a regression. `db-parity` still owed and D59 is the first finding to land in that gap | [review] · [findings] · [ledger](archive/044-045-observability/ledger.md) · [summary] |
| 2026-07-31 | 044 | v1 discovery (full, 9 of 11 manifest lenses) | request-changes | 9/14/9/7 | First review of bolts 044+045 (observability). **9 High**, in three clusters: (a) 🔴 D1 — the `/metrics` IP allow-list compares the TCP peer, which behind the repo's own Caddy edge is always the proxy IP, so allow-listing it (the only way scraping works) opens the endpoint to any anonymous caller; (b) 🔴 D2/D3/D4 — Sentry transactions bypass the scrubber entirely, the scrubber never touches the query string (admin order-search emails, email-confirmation tokens), and header matching is case-sensitive so HTTP/2 lowercase `x-guest-token` survives — all three reproduced live against the real SDK; (c) 🔴 D5/D6/D7 + D8/D9 — per-route sampling can never match a route key, "errors are always sampled" is dead code (`OnEnd` is skipped for dropped spans), whole payment-webhook branches record nothing, and the tests cannot see any of it: deleting `SetBeforeSend`+`SendDefaultPii=false` leaves 32/32 green, deleting `FotoMetrics.OrdersCreated.Add` leaves 1001 green. Suite is also order-dependent (D19: process-wide env vars in static ctors under parallel xUnit). Scope excluded the comment sweep and the `reviews/**` changes riding the same branch. db-parity + frontend-ux **owed, not waived**. 37 agents / ~2.26M tok; 81 raw → 39 canonical; 2 guard + 25 trace skeptics | [review](archive/044-045-observability/review-v1.md) · [findings] · [ledger](archive/044-045-observability/ledger.md) · [summary](archive/044-045-observability/summary-v1.md) |
| 2026-07-29 | system | v1 system review (main agent + 2 independent checkers) | approve-with-followups | 0/5/5/4 | First review of the review system itself. Stands: undeviated cert path 0 completions (SF1) · no quiet full pass ever observed, README oversells "certified" (SF2) · certified twice under a stop rule whose gating experiment never ran (SF14) · no post-cert escape tracking (SF16) · evidence branches local-only, no tags (SF4) · 371 comment-citation blinding leaks in 118 files (SF9). Refuted by the defense checker: SF10, SF11 (kept, never suppressed). First cross-target roll-ups: ~32.85M recorded pass tokens · 125 serious named (~293k tok/serious) · 234 fixes verified, 6 reopened (2.6%) · checker cost ~194k | [review](system/review-v1.md) · [summary](system/summary-v1.md) |
| 2026-07-29 | 015 | v6 fix round (test-only) + **loop closed** | — (owner sign-off) | 0/0/0/0 | All 4 reopens closed (D27/D39/D71/D79) plus the D68/D78 gaps, each proven to redden on revert with zero collateral (6 backend + 4 frontend predicted failures, all matched). No production behaviour changed; two test-enabling source edits. `FakeTimeProvider` now fakes timers — the D82 test had been sleeping 30 real seconds, so backend went 916/30 s → **921/4 s**. Frontend 460. **Closed without a post-fix blinded pass, on owner sign-off** (README note ²): nothing has searched the fixed code, so the pre-enable checklist (D45, D23, D81, D90–D96) is the binding gate. **Recorded deviation:** fixer == verifier for this round | [resolution](archive/015-sameday-shipping/resolution-v6.md) · [ledger](archive/015-sameday-shipping/ledger.md) |
| 2026-07-29 | 015 | v6 verification | approve-with-followups | 0/0/1/6 | **37 verified, 4 reopened** — D27/D39/D71/D79 vanish silently (both suites green with the fix deleted); 19 backend + 6 frontend reverts, failure set predicted before each run and matched exactly. **D65's DI cycle reproduced** (`ValueFactory attempted to access the Value property` in `DefaultHttpClientFactory.CreateHandler`) — the `Enabled=true` flip would have thrown pre-fix. Gaps recorded on D68 (persist-fail leg) and D78 (an assertion that can't fail in Angular). All 7 non-fixed dispositions upheld. Comment rule clean (0 hits added / 9 removed). Backend 916 / frontend 457. **Escalates to a fresh certification** — full-loop-tier touches + 4 reopens re-arm the loop | [review] · [resolution](archive/015-sameday-shipping/resolution-v5.md) · [ledger](archive/015-sameday-shipping/ledger.md) · [summary] |
| 2026-07-28 | 015 | v5 certification (single-pass deviation) | approve-with-followups | 0/17/19/6 | **Certified** — no serious defect survives (0 High, 0 fix-caused regression, 0 reopened); loop closes. New findings all dormant behind two false flags = pre-enable checklist (D55–D89). 30 confirmed / 3 plausible / 4 re-raises re-affirmed (D50/D23/D29/D39) / 1 refuted. D45 vendor-idempotency residual re-confirmed+accepted. Platform 500 wave crippled run 1 (34/64 agents); resumed from cache. ~5.17M tok across both attempts | [review](archive/015-sameday-shipping/review-v5.md) · [findings] · [ledger](archive/015-sameday-shipping/ledger.md) · [summary](archive/015-sameday-shipping/summary-v5.md) |
| 2026-07-27 | 015 | v4 verification | approve-with-followups | 0/0/0/0 | v3 fix round held: 3 blockers (D43/D44/D45) revert-and-rerun (each reddens exactly its own test, zero collateral); 17 findings via the fix-diff micro-review (2 independent agents, clean incl. no-masked-tests) + inspection; D45 also approach-checked. 0 reopened. D50 deferred. Backend 898 / frontend 452 green. Single-pass certification next | [review] · [resolution](archive/015-sameday-shipping/resolution-v3.md) · [ledger](archive/015-sameday-shipping/ledger.md) |
| 2026-07-27 | 015 | v3 certification pair (2 blinded full passes) | request-changes | 3/8/1/0 | **NOT certified.** 3 High blockers (D43 canContinue dead-ends Easybox; D44 timeout kills tracking; D45 AWB per-order-guard) → **fix round resolved `5fc330b`** (all blockers+mediums+8 backlog; D45 durable claim approach-checked; micro-review clean). D31 re-opened+fixed. Caught a v1 regression the same-session v2 missed. 82 agents / ~4.6M tok | [review](archive/015-sameday-shipping/review-v3.md) · [resolution](archive/015-sameday-shipping/resolution-v3.md) · [ledger](archive/015-sameday-shipping/ledger.md) · [summary](archive/015-sameday-shipping/summary-v3.md) |
| 2026-07-27 | 015 | v2 verification | approve-with-followups | 0/0/0/0 | 21/21 fixes verified (12 revert-and-rerun, clean attribution + zero collateral; 9 judgment/inspection); 0 reopened. Backend 893 / frontend 451 green. Certification pair owed | [review] · [resolution](archive/015-sameday-shipping/resolution-v1.md) · [ledger](archive/015-sameday-shipping/ledger.md) |
| 2026-07-27 | 015 | v1 discovery (full 11-lens) | request-changes | 5/14/16/6 | 🔴 D1 constant AWB idempotency key (breaks ADR-015) + D2 dup-create + D3 tracking DbContext share + D4/D5 Easybox mapping → fix round; 1 refuted; 48 agents / ~2.58M tok | [review](archive/015-sameday-shipping/review-v1.md) · [findings] · [ledger](archive/015-sameday-shipping/ledger.md) |
| 2026-07-22 | 043 | v9 certification (single-pass deviation) | approve-with-followups | 0/10/14/6 | **Certified** — 0 High, 0 regression; owner triage 2026-07-27: D83 fixed `d041295` · D84 wont-fix (EuPlatesc removal planned) · D85 → 3-env; D86–D90 backlog | [review](archive/043-cloud-storage-provider/review-v9.md) · [resolution](archive/043-cloud-storage-provider/resolution-v9.md) · [ledger](archive/043-cloud-storage-provider/ledger.md) |
| 2026-07-22 | 043 | v8 verification | approve-with-followups | 0/0/0/0 | 10/10 v7 fixes non-vacuous; both deferrals upheld; 0 reopened | [review] · [resolution](archive/043-cloud-storage-provider/resolution-v7.md) |
| 2026-07-22 | 043 | v7 certification pair | request-changes | 1/11/17/5 | **Not certified**: 🔴 D49 S3-retry data loss + D50 shared-upload class → fix round (all 8 serious fixed same day) | [review](archive/043-cloud-storage-provider/review-v7.md) · [resolution](archive/043-cloud-storage-provider/resolution-v7.md) |
| 2026-07-22 | 043 | v6 verification | approve-with-followups | 0/0/0/0 | D36/D38 held; 12 deferrals upheld; loop quiet → certification | [review] · [resolution](archive/043-cloud-storage-provider/resolution-v5.md) |
| 2026-07-20 | 043 | v5 delta | approve-with-followups | 0/3/10/1 | v3 fix round was fix-generative (promotion sweep + lightbox clusters); D36+D38 fixed, rest → 035/backlog | [review](archive/043-cloud-storage-provider/review-v5.md) · [resolution](archive/043-cloud-storage-provider/resolution-v5.md) |
| 2026-07-20 | 043 | v4 verification | approve-with-followups | 0/0/1/0 | 14/14 v3 fixes held; new Low NF1 → loop not quiet | [review] · [resolution](archive/043-cloud-storage-provider/resolution-v3.md) |
| 2026-07-14 | 043 | v3 delta (+ owed frontend pass) | approve-with-followups | 0/8/10/0 | v1 fix round fix-generative; 14 fixed / 4 deferred | [review](archive/043-cloud-storage-provider/review-v3.md) · [resolution](archive/043-cloud-storage-provider/resolution-v3.md) |
| 2026-07-14 | 043 | v2 verification | approve-with-followups | 0/0/0/0 | 14 verified · 4 decisions upheld · 0 reopened | [review] · [resolution](archive/043-cloud-storage-provider/resolution-v1.md) |
| 2026-07-14 | 043 | v1 discovery (lean, 5 lenses) | request-changes | 1/6/11/0 | 🔴 F1 admin ZIP read local-only storage; 14 fixed / 2 wont-fix / 2 deferred | [review](archive/043-cloud-storage-provider/review-v1.md) · [resolution](archive/043-cloud-storage-provider/resolution-v1.md) |
| 2026-07-14 | 042 | v9 verification | approve-with-followups | 0/0/0/0 | 8 verified · 20 deferrals reaffirmed · 0 reopened; loop quiet | [review] · [resolution](archive/042-thumbnail-cache/resolution-v8.md) |
| 2026-07-14 | 042 | v8 discovery (3rd full pass) | approve-with-followups | 0/7/17/4 | Decay 32→24→13, 15/28 re-raises; headline D85 SplitQuery mis-paging; 8 fixed, tail deferred | [review](archive/042-thumbnail-cache/review-v8.md) · [resolution](archive/042-thumbnail-cache/resolution-v8.md) |
| 2026-07-14 | 042 | v7 verification | approve-with-followups | 0/0/0/0 | 7 verified · deferrals upheld · 0 reopened | [review] · [resolution](archive/042-thumbnail-cache/resolution-v6.md) |
| 2026-07-14 | 042 | v6 discovery (2nd full pass) | approve-with-followups | 0/8/17/4 | 24 new, mostly fix-generated residuals; 4+2 fixed, tail deferred | [review](archive/042-thumbnail-cache/review-v6.md) · [resolution](archive/042-thumbnail-cache/resolution-v6.md) |
| 2026-07-14 | 042 | v5 verification | approve-with-followups | 0/0/0/0 | all 26 v4 fixes non-vacuous; 3 follow-ups (2 doc fixed) | [review] · [resolution](archive/042-thumbnail-cache/resolution-v5.md) |
| 2026-07-14 | 042 | v4 discovery (1st scripted full pass) | approve-with-followups | 0/11/14/7 | 110 agents / ~3.5M tok (the skeptic-calibration datapoint); 26 fixed | [review](archive/042-thumbnail-cache/review-v4.md) · [resolution](archive/042-thumbnail-cache/resolution-v4.md) |
| 2026-07-14 | 042 | v1–v3 (initial review + follow-ups) | approve-with-followups | 3/8/14/3 (v1 set) | 26 verified (v2); NEW-1/2/4 verified (v3); NEW-3 → 043 | [review] · [resolution](archive/042-thumbnail-cache/resolution-v2.md) |
| 2026-07-04 | 035 | v8–v10 (fresh audit + verification) | approve-with-followups | 0/2/9/7 (v8) | v8 found 18 on code v7 called clean; 14 verified · 4 accepted-deferred · 0 open | [review] · [resolution](archive/035-payment-idempotency/resolution-v8.md) |
| 2026-06-19 | 035 | v1–v7 (pre-system loop) | approved (v7 — verdict later shown premature) | 15 (v1) | 13 verified · 2 deferred; the loop that taught the two-loops split ([rationale](rationale.md)) | [review] · [resolution](archive/035-payment-idempotency/resolution-v5.md) |

System improvements backlog: moved to
[self-driving-loop-design.md](self-driving-loop-design.md) (tools table + backlog).
