# This directory holds local, untracked secret material (dev keys, *.pem).
# Everything here except this placeholder is gitignored (see .gitignore).
# Generate dev JWT keys with: scripts/gen-dev-keys.sh  (or .ps1 on Windows)
